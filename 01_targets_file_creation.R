# Load targets library
library(targets)

# 2. Run use_targets() to create the _targets.R file to CONFIGURE and SETUP the PIPELINE
# After installing the package, we load targets “library(targets)”. 
# Then our first step is to run “use_tergets()” function
use_targets()