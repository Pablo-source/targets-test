# using_targets_pipeline.R

# At this stge we start by loading {targets} to use specific functions 
# to run the pipeline we have just created in the _targets.R file

# 1. Load targets library
library(targets)

# 2. 